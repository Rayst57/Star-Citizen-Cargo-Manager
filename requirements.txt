# ── Core planner (stdlib only) ──────────────────────────────────
# sqlite3, json, dataclasses, collections — all stdlib

# ── UI ──────────────────────────────────────────────────────────
PySide6>=6.7

# ── AI / API ────────────────────────────────────────────────────
openai>=1.30              # STT (Whisper), LLM, TTS
keyring>=24               # API key storage in Windows Credential Manager
httpx>=0.27               # cancellable async HTTP

# ── Screen capture (From Screenshot button on AddContract) ──────
mss>=9.0                  # cross-platform fast screen / monitor capture
pygetwindow>=0.0.9        # window enumeration for the "pick a window" picker

# ── Dev / test / packaging ──────────────────────────────────────
pytest>=8.2
pyinstaller>=6.5

# ── Voice subsystem (optional, v2) ──────────────────────────────
# Voice listening / wake-word / PTT are scaffolded but not wired
# in v1. Install the extras with:
#     pip install -r requirements-voice.txt
# (kept separate because pygame doesn't yet have wheels for the
# very latest Python releases and would block a v1 install.)
